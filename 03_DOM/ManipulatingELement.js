// Inline style has high specificity in comparison to external css file. 


// const h1=document.querySelector('h1')

// h1.style.color='Red'
// h1.style.backgroundColor='yellow'
// const anchorTag=document.querySelectorAll('a')

/* anchorTag.forEach((linksColor)=>{
    // linksColor.style.color='teal'
    // linksColor.style.fontFamily='cursive'
    // linksColor.style.textDecorationLine='none'
    // linksColor.style.fontWeight='700'


    //below way is more optimised than above. 

    // css text property-> defining multiple css properties inside backticks. 

    // linksColor.style.cssText=`  
    // color: teal;
    // font-family: cursive;
    // font-size: 18px;
    // font-weight: 700;
    // text-decoration-line:none;`



    // using set attribute property.
    // linksColor.setAttribute('class','green-link')



    // classList method-> adding multiple classes to an element.
    // linksColor.classList.add('green-link','wavy-link')
 
}) */

    // set and get attribute. 

// get attribute-> It is a method which is used to retrieve the value of a specified attribute from an element. 

// const anchor1=document.querySelector('a')
// anchor1.getAttribute('href') // 'https://en.wikipedia.org/wiki/Graphical_user_interface'
// anchor1.setAttribute('href','https://www.google.co.in/')


// className property-> This attribute defines one or more class Names for the element. these classNames can be used for styling the element with css. 


// const heading1=document.querySelector('h1')
// heading1.className+=' heading1'
// console.log(heading1.className) // 'css-image heading1'


